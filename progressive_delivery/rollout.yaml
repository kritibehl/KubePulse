apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: kubepulse-progressive-rollout
spec:
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: {duration: 60}
        - setWeight: 25
        - pause: {duration: 60}
        - setWeight: 50
