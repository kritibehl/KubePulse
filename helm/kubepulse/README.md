# KubePulse Helm Chart

Minimal Helm chart for packaging KubePulse as a Kubernetes release-safety validation service.

Includes:
- Deployment
- Service
- readiness probe
- liveness probe
- resource requests/limits

This chart is intended as a deployability artifact and not a production-hardened Helm release.
