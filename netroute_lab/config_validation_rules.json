{
  "rules": {
    "required_node_fields": ["id", "role", "region"],
    "required_link_fields": ["source", "target", "latency_ms", "packet_loss_pct", "capacity_mbps"],
    "max_latency_ms": 80,
    "max_packet_loss_pct": 2.0,
    "min_capacity_headroom_pct": 20,
    "required_routes": ["checkout_path", "inventory_path", "payments_path", "dns_path"]
  }
}
